# ============================================
# Environment Configuration
# ============================================

# Server Configuration
HTTP_PORT=8080
GRPC_PORT=9090

# PostgreSQL Database (Single DB for both services)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=order_inventory
DB_SSLMODE=disable

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Kafka Configuration
KAFKA_BROKERS=localhost:9092