yamlApplicationConfig:
  kafka:
    clusters:
      - name: yaml
        bootstrapServers: kafka-kafka-bootstrap.order-inventory.svc:9092
  auth:
    type: disabled
  management:
    health:
      ldap:
        enabled: false